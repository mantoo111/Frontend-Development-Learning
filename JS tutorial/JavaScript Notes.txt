                                                       JavaScript:
including js in your code:
 <script src="./script.js"></script>

variables:
letters,digits,underscore& $ sign allowed
must begin with @,$,_,or a letter
js is case sensitive

var and let:
var is a globally scoped while let and const are block scope
var can be updated & redeclared witin its scope
let can be updated but not redirected
const can neither be updated nor be redeclared

creating objects:
 objects are a fundamental part of the language, used to store collections of data and more complex entities.

let person = {
    firstName: "John",
    lastName: "Doe",
    age: 30,
    hobbies: ["reading", "sports"],
    greet: function() {
        console.log("Hello!");
    }
